# Base URL of the vinted-backend API, no trailing slash.
VITE_API_URL=http://localhost:3000

# Stripe publishable key (safe to expose client-side), from the Stripe dashboard.
VITE_STRIPE_PUBLISHABLE_KEY=pk_test_...
